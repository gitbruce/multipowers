# Multipowers Docs Moved

Multipowers customization docs are now located at:

- `custom/docs/tool-project/README.md` (tool maintainers)
- `custom/docs/target-project/README.md` (target project users)

Use scripts from:

- `custom/scripts/`
