# Superpowers Upstream Reference

This folder stores the selected upstream command and skill Markdown synced from:
https://github.com/obra/superpowers

Sync command:

```text
go run ./cmd/mp-devx --action sync-superpowers
```

Only the approved mainline-related assets are mirrored here. The generated
plugin surface should consume these files as upstream source-of-truth for
same-function workflow text.
