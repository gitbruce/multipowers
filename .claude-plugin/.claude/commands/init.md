---
command: init
description: "Guided setup for required /mp:init artifacts"
---

# /mp:init

Thin wrapper role: `initializer`.

Runtime bridge:

`${CLAUDE_PLUGIN_ROOT}/bin/mp init --dir "$PWD" --prompt "$ARGUMENTS" --json`
