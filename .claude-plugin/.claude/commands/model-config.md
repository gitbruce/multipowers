---
command: model-config
description: "Manage per-user model configuration"
---

# /mp:model-config

Thin wrapper role: `initializer`.

Runtime bridge:

`${CLAUDE_PLUGIN_ROOT}/bin/mp model-config --dir "$PWD" --prompt "$ARGUMENTS" --json`
