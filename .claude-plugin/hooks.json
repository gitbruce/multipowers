{
  "SessionStart": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "${CLAUDE_PLUGIN_ROOT}/bin/multipowers hook --event SessionStart --dir \"${CLAUDE_PROJECT_DIR}\" --json"
        }
      ]
    }
  ],
  "UserPromptSubmit": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "${CLAUDE_PLUGIN_ROOT}/bin/multipowers hook --event UserPromptSubmit --dir \"${CLAUDE_PROJECT_DIR}\" --json"
        }
      ]
    }
  ],
  "PreToolUse": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "${CLAUDE_PLUGIN_ROOT}/bin/multipowers hook --event PreToolUse --dir \"${CLAUDE_PROJECT_DIR}\" --json"
        }
      ]
    }
  ],
  "PostToolUse": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "${CLAUDE_PLUGIN_ROOT}/bin/multipowers hook --event PostToolUse --dir \"${CLAUDE_PROJECT_DIR}\" --json"
        }
      ]
    }
  ],
  "Stop": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "${CLAUDE_PLUGIN_ROOT}/bin/multipowers hook --event Stop --dir \"${CLAUDE_PROJECT_DIR}\" --json"
        }
      ]
    }
  ],
  "SubagentStop": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "${CLAUDE_PLUGIN_ROOT}/bin/multipowers hook --event SubagentStop --dir \"${CLAUDE_PROJECT_DIR}\" --json"
        }
      ]
    }
  ]
}
